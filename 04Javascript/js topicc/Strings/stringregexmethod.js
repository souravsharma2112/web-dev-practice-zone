// Regex Methods in Strings:

// .match(regex) – Finds matches.
// .matchAll(regex) – Returns all matches as an iterator.
// .search(regex) – Returns index of first match.
// .replace(regex, newStr) – Replaces text using regex.
// .replaceAll(regex, newStr) – Replaces all occurrences.
// .split(regex) – Splits based on regex.